---

- name: "Create directory for {{ directory_type | default('www') }} - {{ item }}"
  ansible.builtin.file:
    path: "{{ nginx_service_root_path }}/{{ item }}"
    state: directory
    mode: '0755'
    owner: root
    group: root
